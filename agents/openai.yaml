interface:
  display_name: "LP Video Analysis"
  short_description: "Analyze videos into summaries, indexes, reports, and Q&A context."
  default_prompt: "Use $video-understanding-skill to analyze this video, create video_analysis.json, and produce useful derived outputs."

policy:
  allow_implicit_invocation: true
